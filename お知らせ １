说明：
1、因为是core，因为是neko下的core，所以囊括了想要的东东，包括“宝物”和“垃圾”，不会随便删除任何东西，只要它会被用到
2、发布的jar包在10M之前决不拆分子项目，即使拆分命名也是nekocore-*.jar
3、版本号按照标准版本号定义，当版本号第二部分开始变动时，表示可能与前一个版本api不兼容，不兼容的情况需要自行解决
4、使用的Java版本为当前java版本，如果一个java版本发布超过一年，则开始转到此版本，并行发布两个版本的库，如果距下一个java版本发布还有一年，则停止早些版本的发布，仅保留当前最新版本的库
5、当java版本更新时，由于新api与本库中的api拥有相同功能时，如果新api可以完全代替本库api，则删除本库中的api，否则将两个api合并，变成更好的api，原则即取所需要的，取最好的
6、api变更前至少两个小版本需要声明何时开始api变更，但当变更api的版本发布时，无需特别注意api变化
7、api变更一般仅限于对当前api的改变，对新加的api来说不需要太注意
8、新增加的api如果需要提供试用，且不保证正式版中不变，则可暂时放入net.moetang.nekocore._temp包下，以表示此api处于非正式状态，未来有可能变更
